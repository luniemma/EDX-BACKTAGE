{
  "name": "${{ values.name }}",
  "version": "0.1.0",
  "private": true,
  "description": "${{ values.description }}",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  }
}
