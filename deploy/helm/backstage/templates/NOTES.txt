Thanks for installing {{ .Chart.Name }} ({{ .Chart.Version }}).

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Image:     {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}

{{- if .Values.ingress.enabled }}
Ingress:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
The API Service is ClusterIP-only. Port-forward it locally:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "backstage.fullname" . }} 8080:{{ .Values.service.port }}

Then hit http://localhost:8080/health
{{- end }}

{{- if .Values.postgres.enabled }}

An in-cluster Postgres has been deployed at {{ include "backstage.postgresFullname" . }}:5432.
This is intended for dev/preview environments only — use a managed DB in production.
{{- end }}
